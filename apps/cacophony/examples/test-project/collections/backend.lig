-- backend.lig
-- module backend

let config = {
  name = "backend",
  type = "service",
  
  deploy = {
    replicas = 2,
    resources = {
      cpu = "500m",
      memory = "512Mi"
    },
    
    ports = [{
      container = 80,
      service = 8080,
      protocol = "http"
    }]
  },
  
  environment = {
    "NODE_ENV" = "production"
  }
};

export config;
