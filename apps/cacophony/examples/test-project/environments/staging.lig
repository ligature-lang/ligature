-- staging.lig
-- module staging

let overrides = {
  variables = {
    "ENV" = "staging"
  },
  
  collections = {
    app = {
      replicas = 1,
      resources = {
        cpu = "100m",
        memory = "128Mi"
      }
    }
  }
};

export overrides;
